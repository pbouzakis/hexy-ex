"use strict";

import domain from "../domain";

// First implementation always return the same in memory hardcoded place :)
export default function viewPlacesToEat(presenter) {
    let repo = domain.getPlacesRepo();
    
    return () => {
        let places = repo.fetchAll();
        presenter.displayResults(places);
    };
};
