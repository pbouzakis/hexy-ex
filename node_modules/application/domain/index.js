"use strict";

let _resources = {};

// Dead simple container for actions.

export function inject(resources) {
    Object.assign(_resources, resources);
}

export function getPlacesRepo(name) {
    return _resources.placesRepo;
}
